package com.vertexacademy.hello;

import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;

import java.util.Arrays;
import java.util.Random;

import static org.junit.Assert.*;

/**
 * Created by RASTA on 23.04.2016.
 */
public class SorterTest {

/*    private static Sorter sorter;

    private int[] unsortedArr = new int[10];

    private static Random random;

    @BeforeClass
    public static void init() {
        sorter = new SorterImpl();
        random = new Random();
    }

    @Before
    public void initArr() {
        for (int i = 0; i < unsortedArr.length; i++) {
            unsortedArr[i] = random.nextInt();
        }
    }

    @Test
    public void sort() throws Exception {
        int[] sortedArr = sorter.sort(unsortedArr);
        Arrays.sort(unsortedArr);
        assertArrayEquals(unsortedArr, sortedArr);

    }*/
}